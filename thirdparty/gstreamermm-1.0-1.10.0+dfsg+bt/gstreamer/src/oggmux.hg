// Generated by generate_plugin_gmmproc_file. Don't edit this file.


#include <gstreamermm/element.h>
#include <gstreamermm/preset.h>
_DEFS(gstreamermm,gst)

_CONFIGINCLUDE(gstreamermmconfig.h)
_IS_DEPRECATED

namespace Gst
{

/** A Wrapper for the oggmux plugin.
 * Please note that not all plug-ins are available on all systems so care
 * must be taken that they exist before they are used otherwise there will
 * be errors and possibly a crash.
 *
 * @ingroup GstPlugins
 * @deprecated Plugins API is deprecated, and should not be used in a new code.
 */
class OggMux
: public Gst::Element, public Gst::Preset
{
  _CLASS_GOBJECT(OggMux, GstOggMux, GST_OGG_MUX_GSTREAMERMM, Gst::Element, GstElement)
  _IMPLEMENTS_INTERFACE(Gst::Preset)

  _IS_GSTREAMERMM_PLUGIN
protected:
  OggMux();
  explicit OggMux(const Glib::ustring& name);

public:
  /** Creates a new oggmux plugin with a unique name.
   */
  _WRAP_CREATE()

  /** Creates a new oggmux plugin with the given name.
   */
  _WRAP_CREATE(const Glib::ustring& name)

  _WRAP_PROPERTY("max-delay", guint64)
  _WRAP_PROPERTY("max-page-delay", guint64)
  _WRAP_PROPERTY("max-tolerance", guint64)
  _WRAP_PROPERTY("skeleton", bool)
};

} // namespace Gst
