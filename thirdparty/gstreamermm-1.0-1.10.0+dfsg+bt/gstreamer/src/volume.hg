// Generated by generate_plugin_gmmproc_file. Don't edit this file.


#include <gstreamermm/basetransform.h>
#include <gstreamermm/streamvolume.h>
_DEFS(gstreamermm,gst)

_CONFIGINCLUDE(gstreamermmconfig.h)
_IS_DEPRECATED

namespace Gst
{

/** A Wrapper for the volume plugin.
 * Please note that not all plug-ins are available on all systems so care
 * must be taken that they exist before they are used otherwise there will
 * be errors and possibly a crash.
 *
 * @ingroup GstPlugins
 * @deprecated Plugins API is deprecated, and should not be used in a new code.
 */
class Volume
: public Gst::BaseTransform, public Gst::StreamVolume
{
  _CLASS_GOBJECT(Volume, GstVolume, GST_VOLUME_GSTREAMERMM, Gst::BaseTransform, GstBaseTransform)
  _IMPLEMENTS_INTERFACE(Gst::StreamVolume)

  _IS_GSTREAMERMM_PLUGIN
protected:
  Volume();
  explicit Volume(const Glib::ustring& name);

public:
  /** Creates a new volume plugin with a unique name.
   */
  _WRAP_CREATE()

  /** Creates a new volume plugin with the given name.
   */
  _WRAP_CREATE(const Glib::ustring& name)

  _WRAP_PROPERTY("mute", bool)
  _WRAP_PROPERTY("volume", double)
};

} // namespace Gst
