// Generated by generate_plugin_gmmproc_file. Don't edit this file.


#include <gstreamermm/element.h>
_DEFS(gstreamermm,gst)

_CONFIGINCLUDE(gstreamermmconfig.h)
_IS_DEPRECATED

namespace Gst
{

/** A Wrapper for the audiorate plugin.
 * Please note that not all plug-ins are available on all systems so care
 * must be taken that they exist before they are used otherwise there will
 * be errors and possibly a crash.
 *
 * @ingroup GstPlugins
 * @deprecated Plugins API is deprecated, and should not be used in a new code.
 */
class AudioRate
: public Gst::Element
{
  _CLASS_GOBJECT(AudioRate, GstAudioRate, GST_AUDIO_RATE_GSTREAMERMM, Gst::Element, GstElement)
  _IS_GSTREAMERMM_PLUGIN
protected:
  AudioRate();
  explicit AudioRate(const Glib::ustring& name);

public:
  /** Creates a new audiorate plugin with a unique name.
   */
  _WRAP_CREATE()

  /** Creates a new audiorate plugin with the given name.
   */
  _WRAP_CREATE(const Glib::ustring& name)

  _WRAP_PROPERTY("in", guint64)
  _WRAP_PROPERTY("out", guint64)
  _WRAP_PROPERTY("add", guint64)
  _WRAP_PROPERTY("drop", guint64)
  _WRAP_PROPERTY("silent", bool)
  _WRAP_PROPERTY("tolerance", guint64)
  _WRAP_PROPERTY("skip-to-first", bool)
};

} // namespace Gst
