// Generated by generate_plugin_gmmproc_file. Don't edit this file.


#include <gstreamermm/basetransform.h>
_DEFS(gstreamermm,gst)

_CONFIGINCLUDE(gstreamermmconfig.h)
_IS_DEPRECATED

namespace Gst
{

_WRAP_ENUM(AudioDitherMethod,GstAudioDitherMethod)
_CONV_ENUM(Gst,AudioDitherMethod)

_WRAP_ENUM(AudioNoiseShapingMethod,GstAudioNoiseShapingMethod)
_CONV_ENUM(Gst,AudioNoiseShapingMethod)

/** A Wrapper for the audioconvert plugin.
 * Please note that not all plug-ins are available on all systems so care
 * must be taken that they exist before they are used otherwise there will
 * be errors and possibly a crash.
 *
 * @ingroup GstPlugins
 * @deprecated Plugins API is deprecated, and should not be used in a new code.
 */
class AudioConvert
: public Gst::BaseTransform
{
  _CLASS_GOBJECT(AudioConvert, GstAudioConvert, GST_AUDIO_CONVERT_GSTREAMERMM, Gst::BaseTransform, GstBaseTransform)
  _IS_GSTREAMERMM_PLUGIN
protected:
  AudioConvert();
  explicit AudioConvert(const Glib::ustring& name);

public:
  /** Creates a new audioconvert plugin with a unique name.
   */
  _WRAP_CREATE()

  /** Creates a new audioconvert plugin with the given name.
   */
  _WRAP_CREATE(const Glib::ustring& name)

  _WRAP_PROPERTY("dithering", Gst::AudioDitherMethod)
  _WRAP_PROPERTY("noise-shaping", Gst::AudioNoiseShapingMethod)
};

} // namespace Gst
