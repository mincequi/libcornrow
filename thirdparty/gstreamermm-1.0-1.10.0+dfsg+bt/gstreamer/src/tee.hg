// Generated by generate_plugin_gmmproc_file. Don't edit this file.


#include <gstreamermm/element.h>
#include <gstreamermm/pad.h>
_DEFS(gstreamermm,gst)

_CONFIGINCLUDE(gstreamermmconfig.h)
_IS_DEPRECATED

namespace Gst
{

_WRAP_ENUM(TeePullMode,GstTeePullMode)
_CONV_ENUM(Gst,TeePullMode)

/** A Wrapper for the tee plugin.
 * Please note that not all plug-ins are available on all systems so care
 * must be taken that they exist before they are used otherwise there will
 * be errors and possibly a crash.
 *
 * @ingroup GstPlugins
 * @deprecated Plugins API is deprecated, and should not be used in a new code.
 */
class Tee
: public Gst::Element
{
  _CLASS_GOBJECT(Tee, GstTee, GST_TEE_GSTREAMERMM, Gst::Element, GstElement)
  _IS_GSTREAMERMM_PLUGIN
protected:
  Tee();
  explicit Tee(const Glib::ustring& name);

public:
  /** Creates a new tee plugin with a unique name.
   */
  _WRAP_CREATE()

  /** Creates a new tee plugin with the given name.
   */
  _WRAP_CREATE(const Glib::ustring& name)

  _WRAP_PROPERTY("num-src-pads", int)
  _WRAP_PROPERTY("has-chain", bool)
  _WRAP_PROPERTY("silent", bool)
  _WRAP_PROPERTY("last-message", Glib::ustring)
  _WRAP_PROPERTY("pull-mode", Gst::TeePullMode)
  _WRAP_PROPERTY("alloc-pad", Glib::RefPtr<Gst::Pad>)
  _WRAP_PROPERTY("allow-not-linked", bool)
};

} // namespace Gst
